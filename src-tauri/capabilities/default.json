{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Pumice — grants dialog access and core APIs",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
